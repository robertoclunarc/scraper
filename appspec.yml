version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/scraper
    overwrite: true
permissions:
  - object: /var/www/scraper
    pattern: "**"
    owner: ec2-user
    group: ec2-user
hooks:
  BeforeInstall:
    - location: startScraping.sh
      timeout: 300
      runas: ec2-user